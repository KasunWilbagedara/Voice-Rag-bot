# Voice-Rag-bot
This project develops a voice-based AI assistant using Retrieval-Augmented Generation (RAG). It converts speech to text, retrieves relevant information from a knowledge base, generates an accurate response using an LLM, and returns the answer as natural speech in real time.
